# 臺灣公民科技行動指引網站

⚠️ **這個 repo 是公開的**（`github.com/Legendream/g0v.civictech-pipeline`）。
commit 就是發布，而且留在 git 歷史裡清不掉。

上線網址：`https://civictech-guide.claire-cheng.com`

## 這個站在做什麼

一條敘事線：**問卷發現社群卡在哪 → 入口其實已經存在（臺灣公民科技資料庫）
→ 缺的是有人把經驗寫下來 → 所以做了一條半自動的寫作生產鏈，整條攤開讓人拿走。**

公民科技是這次的示範領域，方法本身跟領域無關。這句話放敘事末端，
不放首頁開頭：開頭就講通用，會變成一個沒有主角的方法論網站。

主要讀者是「對台灣公民科技好奇的人」，想拿去用的人是第二層。

## 三個 repo 的分工

本機資料夾名跟 repo 名不一定一樣，**認 repo 名不要認資料夾名**：

| 本機資料夾 | repo | 放什麼 |
|---|---|---|
| `~/Claude/g0v.civictech-pipeline` | `g0v.civictech-pipeline`（這裡） | 網站本體：敘事、頁面、留言區 |
| `~/Claude/g0v civic tech guide` | `Taiwan-civic-tech-research` | 問卷分析報告的正本 |
| `~/Claude/AI Writing Pipeline` | `AI-Writing-Skill` | 寫作方法資產的正本（SKILL、模板、驗收清單） |

那兩個舊資料夾的名字**不要為了整齊去改**：Claude Code 的專案記憶與對話紀錄
綁在資料夾路徑上（`~/.claude/projects/<路徑轉出來的目錄>/`），改名會讓它們對不上。
git 不在乎資料夾叫什麼，但那一層在乎。

**正本只有一份。** 這個站對報告與流程只做導讀與連結，不複製全文。
複製會分岔，然後兩個版本說不一樣的話，而且你不會記得回頭改。

## PR 的節奏：一個批次一個分支一個 PR

**不要把 merge 當成自己的除錯步驟。**

分支推上去 Cloudflare 就會產生預覽網址（Preview builds 已開啟）。
需要「實際跑起來才驗得到」的東西，在分支的預覽網址上驗完，
最後才開一個 PR。批次內的所有修正累積在同一個分支。

**診斷證據**：2026-09-05 建站當天開了六個 PR，其中四個是部署鏈路的修正
（Deploy command 填錯、Worker 名字對不上、網域被別的站佔用）。
每一個都逼 Claire 走一輪 merge，因為當時誤以為只有 merge 到 `main` 才會部署。
預覽網址那時候就已經開著，沒用上。

例外：真的必須 merge 到 `main` 才驗得到的事，先說明理由再問，
不要默默把 merge 排進自己的除錯流程。

其餘照全域 `CLAUDE.md`：不直接 commit 到 `main`、動手前先寫驗收條件、
Claire 自己按 merge。

## 雙語架構的四條原則

目前只做中文，架構保留英文擴充。**這四條是刻意的，不要順手改掉**：

1. **內容跟版型分家**：正文是 `src/pages/<語言>/*.md`，介面字串在 `src/i18n/<語言>.json`。
   版型檔裡不出現寫死的中文
2. **翻譯單位是頁，不是句**：允許某頁只有中文。缺英文時那頁不出現在英文導覽，
   不要把中文原文丟給英文讀者
3. **網址一律帶語言前綴**：`/zh/why/`，不是 `/why`。根目錄轉到 `/zh/`
4. **留言資料表有 `locale` 欄位**：現在只會有 `zh`，但欄位要從第一天就在，
   之後才不用做資料遷移

翻不翻的判準是**它還會不會變**：已定稿不再修改的（問卷報告）可以翻；
還在演化的（寫作流程規則本體）不翻，翻了等於認養一份會一直分岔的雙胞胎。

## 部署

Astro 產生靜態頁，部署在 Cloudflare Workers。**後台那幾格設定與踩過的坑寫在
`docs/部署設定.md`，改部署相關的東西之前先讀那份。**

自訂網域宣告在 `wrangler.jsonc` 的 `routes`，不要改在後台，兩邊都設會打架。

## 文案原則

1. **MAPS**：精簡、具體情境、用對照表讓人辨模式、由簡入繁
2. **正向表述**：講「可以怎麼做」，不講「什麼行不通」
3. **假設讀者沒有背景**：不出現內部細節（第幾條規則、幾條驗收條件），
   也不要技術名詞（知識庫、子代理、脈絡、repo）
4. **全文無破折號**
